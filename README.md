# Routine dashboard

A calm, graphite-first life dashboard reconstructed from the supplied Figma Make file. The interface includes nine routed modules and a fully interactive task workspace with smart views, task details, date and time controls, priorities, lists, tags, habits, and a weekly summary document.

## Run locally

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
